<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Document">

    <resultMap id="Document" type="com.zhaodesong.Edocumentsystem.po.Document">
        <result property="id" column="id"/>
        <result property="teamId" column="team_id"/>
        <result property="name" column="name"/>
        <result property="version" column="version"/>
        <result property="accountId" column="account_id"/>
        <result property="type" column="type"/>
        <result property="parentId" column="parent_id"/>
        <result property="level" column="level"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
	    <![CDATA[
        id ,team_id ,name ,version ,account_id ,type ,parent_id ,level ,create_time ,update_time 
	    ]]>
	</sql>
	
    <insert id="insert"  useGeneratedKeys="true" keyProperty="id"  >
    <![CDATA[
        INSERT INTO document (
	        id  ,
	        team_id  ,
	        name  ,
	        version  ,
	        account_id  ,
	        type  ,
	        parent_id  ,
	        level  ,
	        create_time  ,
	        update_time  
        ) VALUES (
        	#{id, jdbcType=BIGINT} ,
        	#{teamId, jdbcType=INTEGER} ,
        	#{name, jdbcType=VARCHAR} ,
        	#{version, jdbcType=INTEGER} ,
        	#{accountId, jdbcType=INTEGER} ,
        	#{type, jdbcType=BIT} ,
        	#{parentId, jdbcType=BIGINT} ,
        	#{level, jdbcType=TINYINT} ,
        	#{createTime, jdbcType=TIMESTAMP} ,
        	#{updateTime, jdbcType=TIMESTAMP} 
        )
    ]]>
    </insert>

	<insert id="insertNotNull"  useGeneratedKeys="true" keyProperty="id"  >
			<![CDATA[
			INSERT INTO document (
				<if test="null!=id">
					id  ,
				</if>
				<if test="null!=teamId">
					team_id  ,
				</if>
				<if test="null!=name">
					name  ,
				</if>
				<if test="null!=version">
					version  ,
				</if>
				<if test="null!=accountId">
					account_id  ,
				</if>
				<if test="null!=type">
					type  ,
				</if>
				<if test="null!=parentId">
					parent_id  ,
				</if>
				<if test="null!=level">
					level  ,
				</if>
				<if test="null!=createTime">
					create_time  ,
				</if>
				<if test="null!=updateTime">
					update_time  
				</if>
			) VALUES (
				<if test="null!=id">
					#{id, jdbcType=BIGINT} ,
				</if>
				<if test="null!=teamId">
					#{teamId, jdbcType=INTEGER} ,
				</if>
				<if test="null!=name">
					#{name, jdbcType=VARCHAR} ,
				</if>
				<if test="null!=version">
					#{version, jdbcType=INTEGER} ,
				</if>
				<if test="null!=accountId">
					#{accountId, jdbcType=INTEGER} ,
				</if>
				<if test="null!=type">
					#{type, jdbcType=BIT} ,
				</if>
				<if test="null!=parentId">
					#{parentId, jdbcType=BIGINT} ,
				</if>
				<if test="null!=level">
					#{level, jdbcType=TINYINT} ,
				</if>
				<if test="null!=createTime">
					#{createTime, jdbcType=TIMESTAMP} ,
				</if>
				<if test="null!=updateTime">
					#{updateTime, jdbcType=TIMESTAMP} 
				</if>
			)
		]]>
	</insert>

   <insert id="insertBatch"   parameterType="java.util.List">
      INSERT INTO document ( <include refid="columns"/>) values
        <foreach collection="list" item="item" separator="," >
    (
        	#{item.id, jdbcType=BIGINT} ,
        	#{item.teamId, jdbcType=INTEGER} ,
        	#{item.name, jdbcType=VARCHAR} ,
        	#{item.version, jdbcType=INTEGER} ,
        	#{item.accountId, jdbcType=INTEGER} ,
        	#{item.type, jdbcType=BIT} ,
        	#{item.parentId, jdbcType=BIGINT} ,
        	#{item.level, jdbcType=TINYINT} ,
        	#{item.createTime, jdbcType=TIMESTAMP} ,
        	#{item.updateTime, jdbcType=TIMESTAMP} 
    )
        </foreach>
    </insert>

    <update id="update" >
    <![CDATA[
        UPDATE document SET
	        team_id = #{teamId, jdbcType=INTEGER} ,
	        name = #{name, jdbcType=VARCHAR} ,
	        version = #{version, jdbcType=INTEGER} ,
	        account_id = #{accountId, jdbcType=INTEGER} ,
	        type = #{type, jdbcType=BIT} ,
	        parent_id = #{parentId, jdbcType=BIGINT} ,
	        level = #{level, jdbcType=TINYINT} ,
	        create_time = #{createTime, jdbcType=TIMESTAMP} ,
	        update_time = #{updateTime, jdbcType=TIMESTAMP} 
        WHERE 
	        id = #{id} 
    ]]>
    </update>

	<update id="updateNotNull" >
			UPDATE document
				<set>
					<if test="null!=teamId">
						team_id = #{teamId, jdbcType=INTEGER} ,
					</if>

					<if test="null!=name and ''!=name ">
						name = #{name, jdbcType=VARCHAR} ,
					</if>

					<if test="null!=version">
						version = #{version, jdbcType=INTEGER} ,
					</if>

					<if test="null!=accountId">
						account_id = #{accountId, jdbcType=INTEGER} ,
					</if>

					<if test="null!=type and ''!=type ">
						type = #{type, jdbcType=BIT} ,
					</if>

					<if test="null!=parentId">
						parent_id = #{parentId, jdbcType=BIGINT} ,
					</if>

					<if test="null!=level">
						level = #{level, jdbcType=TINYINT} ,
					</if>

					<if test="null!=createTime and ''!=createTime ">
						create_time = #{createTime, jdbcType=TIMESTAMP} ,
					</if>

					<if test="null!=updateTime and ''!=updateTime ">
						update_time = #{updateTime, jdbcType=TIMESTAMP} 
					</if>

				</set>
			WHERE
				id = #{id} 
	</update>

    <delete id="delete">
    <![CDATA[
        DELETE FROM document WHERE
        id = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultMap="Document">
	    SELECT <include refid="columns"/>
	    <![CDATA[
	        FROM document 
	        WHERE 
		        id = #{id} 
	    ]]>
    </select>

	<sql id="findWhere">
	    <where>
	    	<choose>
	    		<when test="queryType==0">
			       	<if test="null!=name and ''!=name ">
	   			 		AND name = #{name}
	   			   	</if>
	       		</when>
	       		<otherwise>
				    <if test="null!=name and ''!=name">
				    <!-- 	AND name LIKE '%'||#{name}||'%' oracle -->
			   			AND name LIKE concat('%',#{name},'%')
			   			
			   		</if>
		        </otherwise>
	      	</choose>
		   	<if test="null!=id and ''!=id">
			    AND id = #{id}
			</if>
			<if test="null!=teamId">
				AND team_id = #{teamId}
			</if>
			<if test="null!=version">
				AND version = #{version}
			</if>
			<if test="null!=accountId">
				AND account_id = #{accountId}
			</if>
		   	<if test="null!=type and ''!=type">
			    AND type = #{type}
			</if>
			<if test="null!=parentId">
				AND parent_id = #{parentId}
			</if>
			<if test="null!=level">
				AND level = #{level}
			</if>
		   	<if test="null!=createTimeBegin and ''!=createTimeBegin">
		   		AND create_time >= #{createTimeBegin}
		   	</if>
		   	<if test="null!=createTimeEnd and ''!=createTimeEnd">
		   		AND create_time &lt; #{createTimeEnd}
		   	</if>
		   	<if test="null!=updateTimeBegin and ''!=updateTimeBegin">
		   		AND update_time >= #{updateTimeBegin}
		   	</if>
		   	<if test="null!=updateTimeEnd and ''!=updateTimeEnd">
		   		AND update_time &lt; #{updateTimeEnd}
		   	</if>
	    </where>
	</sql>
	 
    <select id="findPageCount" resultType="long">
        SELECT count(*) FROM document <include refid="findWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultMap="Document">
    	SELECT <include refid="columns"/> FROM document 
		<include refid="findWhere"/>
		<if test="null!=sortColumns and ''!=sortColumns">
			ORDER BY ${sortColumns}
		</if>
    </select>

	
	<select id="find" resultMap="Document">
	    SELECT <include refid="columns"/> FROM document 
		<include refid="findWhere"/>
		<if test="null!=sortColumns">
			ORDER BY ${sortColumns}
		</if>
    </select>
	
</mapper>